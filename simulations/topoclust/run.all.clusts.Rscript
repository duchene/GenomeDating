for(i in 1:length(toposchemes)){
      for(j in 1:3){
      	    trialprocess <- process.gd.sim(toposchemes[[i]], seqlength = length(toposchemes[[i]][[1]]$tip.label) * 20, reps = 10)
	    #print(unlist(trialprocess[[3]], recursive = F))
	    topclusters[[length(topclusters) + 1]] <- topoclustMDS(unlist(trialprocess[[3]], recursive = F), mdsdim = j)
	    names(topclusters)[length(topclusters)] <- paste0(names(toposchemes)[i], "d", j)
	    
      }
}